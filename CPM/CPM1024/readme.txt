This folder contains the "extended" model of CP/M, making possible to build huge executables (up to 496KB code, and up to 448 KB memory space to store data), when using the 512KB RAM + 512KB ROM memory module.

The "example.txt" contains an example of how to build such an executable.
